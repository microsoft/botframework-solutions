{
  "name": "root",
  "private": true,
  "version": "0.0.1-0",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build --parallel --stream",
    "clean": "lerna run clean --parallel --stream"
  },
  "devDependencies": {
    "lerna": "^3.5.1"
  }
}
